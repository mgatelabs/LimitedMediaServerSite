import { MediaRatingPipe } from './media-rating.pipe';

describe('MediaRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
